{% extends 'base.html.twig' %}

{% block title %}myVideoPlayer || Mise à jour
	{{ video.name }}
{% endblock %}

{% block body %}
	<div class="row p2 mx-auto">
		<h1>Mise à jour :
			{{ video.name }}
		</h1>
	</div>
	<div class="row p2 mx-auto">
		{{ include('video/_form.html.twig', {'button_label': 'Mise à jour'}) }}
	</div>
	<div class="row p2">
		<a class="btn btn-dark col-2" href="{{ path('video_index') }}">Retour</a>
	</div>
{% endblock %}
